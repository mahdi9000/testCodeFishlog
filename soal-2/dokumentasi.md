## Untuk Run App
1. install deps dengan `yarn install`
2. dev server dengan `yarn dev`
3. endpoint /sort > sort descending based on item name
4. endpoint /filter?itemName=kuCiNg > filter berdasarkan query params, query required